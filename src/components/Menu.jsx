import React from 'react';

const Menu = () => (
  <div>
    <button
      type="button"
      className="btn btn-success"
    >
      {'Create'}
    </button>
    <button
      type="button"
      className="btn btn-danger"
    >
      {'Remove'}
    </button>
  </div>
);

export default Menu;
